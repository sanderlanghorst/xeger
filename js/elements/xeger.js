/**
 * Xeger
 * Input handler
 */

const
	moduleName = 'xeger',
	cssClasses = {
		module = `.js-${moduleName}`
	}
/**
 * 
 * @param {Array<HTMLElement>} elements 
 */

function init(elements = document.querySelectorAll(cssClasses.module)){

}
export {init};